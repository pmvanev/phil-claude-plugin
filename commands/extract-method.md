---
description: "Extract cohesive code blocks into independent, reusable methods."
argument-hint: "<lines:N-M | method-name | file-path | directory-path>"
mutates: true
allowed-tools: Read, Edit, Write, Glob, Grep, Bash
---

Load and follow the instructions in the `extract-method` skill at `${CLAUDE_PLUGIN_ROOT}/skills/extract-method/SKILL.md`.
