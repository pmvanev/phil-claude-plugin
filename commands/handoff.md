---
description: "Put this session down: record what a fresh session cannot derive — the decisions reached, the approaches ruled out, and the intended next action — stamped with a tree fingerprint so the next session can tell whether it is still current. Writes nothing if the session advanced nothing."
argument-hint: "[\"<what you were doing>\"]"
allowed-tools: Read, Edit, Write, Glob, Grep, Bash, AskUserQuestion, Skill
---

Load and follow the CAPTURE half of the instructions in the `session-handoff` skill at
`skills/session-handoff/SKILL.md`.
