---
description: "Rewrite tests that verify implementation into tests that verify behaviour — a human-approved, behaviour-CHANGING redesign loop where every rewrite carries a coverage-equivalence claim the human validates."
argument-hint: "[--review <path> | <file-path> | <test-id>]"
allowed-tools: Read, Edit, Write, Glob, Grep, Bash, AskUserQuestion, TaskCreate, TaskUpdate
---

Load and follow the instructions in the `redesign-tests` skill at `skills/redesign-tests/SKILL.md`.
