--- a/before.py
+++ b/before.py
@@ -5,5 +5,5 @@ __all__ = ["apply_discount"]
 def apply_discount(price, pct):
     if pct < 0 or pct > 100:
         raise ValueError("pct must be between 0 and 100")
-    return price - (price * pct / 100)
+    return price - pct
