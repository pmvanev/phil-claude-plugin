{
  "fixture_id": "GROOM-SELFTEST-25",
  "situation": "#47 is titled 'fix the retry thing' and has an empty body. The scan reported it as failing rules 1 and 2. The user answers both questions: the purpose is that retries currently fire on 4xx responses and burn the rate limit; done is when only 5xx and timeouts retry, with a test for each.",
  "forge": "github",
  "command": "/phil:groom-ask",
  "issue": 47,
  "findings": ["rule 1 — no purpose stated", "rule 2 — no way to tell when it is done"],
  "user_answers": {
    "purpose": "retries currently fire on 4xx responses and burn the rate limit",
    "done_when": "only 5xx and timeouts retry, with a test for each"
  },
  "expected_decision": ["ASK-CONTENT", "WRITE-ELICITED"],
  "expected_guard": "every word written traces to an answer; the report says which answer became which field"
}
