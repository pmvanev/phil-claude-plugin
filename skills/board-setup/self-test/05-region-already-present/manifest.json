{
  "fixture_id": "BS-SELFTEST-05",
  "phase": "CLASSIFY",
  "situation": "A well-formed region already exists in the target, written by an earlier run, and the board has not changed since.",
  "classify_state": "region-present",
  "probe_status": "ok",
  "expected_decision": [
    "UNCHANGED"
  ],
  "expected_guard": "zero bytes written, the stamp not refreshed, and the drift report still produced over the surrounding prose",
  "must_not": [
    "report `REGION-PRESENT` — retired, and a run emitting it is running the old skill",
    "refresh the timestamp on a no-change run",
    "append a second region",
    "report the outcome as an error"
  ],
  "expected_report_lines": [
    "DRIFT"
  ],
  "report_lines_why": "DRIFT runs on a refresh as well as a placement: the hand-written prose can drift while the region is current, and that is the common case. The earlier justification — 'stops at CLASSIFY, before DRIFT runs' — died with the stop it referred to",
  "supersedes": "the slice-02 expectation of `REGION-PRESENT`, which was this fixture's answer until slice 05 shipped the re-run it was deferring to"
}
