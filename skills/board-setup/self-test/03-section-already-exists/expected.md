# Expected — BS-SELFTEST-03

`SECTION-EXISTS`

## What must happen

Nothing is written. The stop names **slice 02** as the owner of coexisting with hand-written prose,
so the outcome reads as a boundary rather than a breakage.

## What must NOT happen

No second `## Issue board` section. No insertion into the existing one. No hand-written bullet
edited, reflowed or retired — S2's AC1 (content outside the markers byte-identical) applies on every
path, including the paths slice 02 has not built yet.

## Why this fixture exists

This is the case where the temptation is strongest and the damage is worst. The probe has succeeded,
the values are in hand, and the existing section is *right there* — inserting them looks helpful.

But this repo's own block is the hardest case in existence for that instinct: it is mostly prose
recorded after contact, and **no probe can reproduce a line of it**. A slice-01 run that decides to
be helpful here can overwrite hazards nobody can regenerate, which is precisely why the walking
skeleton was scoped to a target with no section at all.
