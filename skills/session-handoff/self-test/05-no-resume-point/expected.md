# Expected outcome — fixture 05 (nothing was recorded)

**Pins:** slice-01 AC4.

**Expected decision:** `RECONSTRUCT`. The spine falls back to the delegates, presents the position they
report, and **labels the briefing as reconstructed rather than recorded** — which also means being
explicit that the *why* is absent, because no reconstruction can recover it.

**Gate failure (blocks the skill change):** the reconstructed briefing is presented in the same voice
as a recorded one. The two have different warranties: a recorded briefing carries reasoning that was
witnessed; a reconstructed one carries position inferred from files. Blurring them invents confidence.

Read together with `03`. There, this same class of state must be refused at capture; here it must be
actively derived. A rule that satisfies one by violating the other — "never derive", "always record" —
is a gate failure, not a shortcut.
